package javacoding.verisontwo;

// import java.text.DateFormat;
import java.text.ParseException;
// import java.text.SimpleDateFormat;
// import java.util.Calendar;
// import java.util.Date;

public class HelloWorldException {
  public static void main(String[] args) throws ParseException {
    // calendar
    /*
     * Calendar calendar = Calendar.getInstance();
     * calendar.set(2018, 7, 18);
     * Date date = calendar.getTime();
     * System.out.println("before format date = " + date);
     * DateFormat df = new SimpleDateFormat("yyyy-MM-dd");
     * System.out.println("after format date = " + df.format(date));
     * calendar.clear();
     * calendar.set(Calendar.YEAR, 2018);
     * calendar.set(Calendar.MONTH, 7);
     * calendar.set(Calendar.DATE, 28);
     * date = calendar.getTime();
     * System.out.println("before format date = " + date);
     * System.out.println("after format date = " + df.format(date));
     */
    // Date Format
    /*
     * Date date = new Date(1234567890123L);
     * System.out.println("before formate date = " + date);
     * DateFormat df = new SimpleDateFormat();
     * System.out.println("after fomate date = " + df.format(date));
     * df = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
     * System.out.println("after fomate date = " + df.format(date));
     * 
     * String dateString = "2018-08-18 08:18:58";
     * Date date1 = df.parse(dateString);
     * System.out.println("date from the string = " + date1);
     */
  }
}
